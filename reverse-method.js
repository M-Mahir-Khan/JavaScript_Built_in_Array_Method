// reverse method 

// reverse() ek JavaScript array method hai jo existing array ke elements ko reverse order mein modify karta hai.

// Is method ka use karke aap array ke elements ko ulta kar sakte hain, matlab jo elements pehle se last the wo ab pehle ho jaate hain aur jo pehle the wo ab last ho jaate hain.

const numbers = [1,2,3,4,5,6]

numbers.reverse()
console.log(numbers); //output : [6, 5, 4, 3, 2, 1]